# AIGC Interactive Character Simulator Godot Client

这是当前前端主线，已经加入“出租屋 -> 街道 -> AI 伴侣商店 -> 回家互动”的基础剧情流程。开场先看剧情，再进入商店生成角色，最后回到出租屋开始互动。

## 当前功能

- 游戏风格主菜单
- 出租屋开场背景
- 弹出式剧情对话
- 街道过渡章节
- AI 伴侣商店背景
- 商店式伴侣挑选界面
- 无图片 API 的占位伴侣档案
- 角色列表和角色资料
- 回家后开启 Chat UI
- 面对面沉浸对话模式
- 可隐藏主界面，只保留角色、背景、对白框和输入
- FastAPI API 调用
- 模块化剧情 pack

## 结构

```text
godot-client/
  project.godot
  assets/
    backgrounds/
      home.png
      aic_shop.png
  scenes/
    main.tscn
  scripts/
    main.gd
    story/
      story_pack_base.gd
      base_game_pack.gd
```

## 打开方式

1. 安装 Godot 4.2 或更新版本。
2. 用 Godot 打开 `godot-client` 文件夹。
3. 运行主场景。
4. 确认 Backend 在运行，默认地址是 `http://127.0.0.1:8000`。

## 基础剧情流程

1. 出租屋：弹出对话介绍主角和目标。
2. 街道：显示出门前往商店的过渡对白。
3. AI 伴侣商店：开启角色创建和 API 配置。
4. 选择角色：创建或选择 AI 伴侣。
5. 回到出租屋：确认带回角色。
6. Chat：开始和生成的 AI 角色互动。
7. 面对面模式：隐藏菜单，对着角色直接说话。

## 无图片 API 测试

当前商店页支持先登记“占位伴侣”：

- 不需要填写图片 API Key
- 不会等待图片生成
- 可以直接测试 DeepSeek / 文本对话 API
- 可以测试角色性格、背景、初始记忆和关系记忆

图片 API 只负责后续生成正式立绘和动作图，不再阻塞进入聊天。

如果点击登记后一直没有反应，客户端会在超时后提示后端没有响应。此时先确认 FastAPI Backend 正在运行，默认地址是：

```text
http://127.0.0.1:8000
```

## 商店挑选界面

AI 伴侣商店现在更接近游戏内挑选流程：

- 展示柜基础型号：温柔陪伴型、活泼好奇型、安静守护型、自定义空白型
- 性别选择
- 年龄限制：21-34 岁
- 画风选择：真人写实、二次元、国漫脸、半写实
- 外貌描述
- 占位预览

点击“登记占位伴侣”后，游戏会先保存一个可聊天角色档案。之后再接入图片 API，就可以把这个档案升级成正式形象。

## 恋爱模拟器方向

回到出租屋后，聊天页左侧会显示“房间里的她”。点击：

```text
隐藏界面，面对面说话
```

会进入沉浸式对话界面，只保留出租屋背景、角色舞台、底部对白框和输入框，更接近恋爱模拟器体验。

## 剧情模块

基础剧情在：

```text
scripts/story/base_game_pack.gd
```

后续 DLC 可以新增自己的剧情 pack，按照 `StoryPackBase` 提供章节数据，再由主场景加载。每个章节可以独立配置：

- 背景图
- 标题
- 说话者
- 对话文本
- 是否显示游戏功能
- 章节结束后的按钮文字
- 进入哪个功能页面

## 素材

当前使用：

- `assets/backgrounds/home.png`：主角出租屋
- `assets/backgrounds/aic_shop.png`：AI 伴侣商店

图片来自本次提供的参考素材，已复制进工程，不依赖 Downloads 文件夹。
